public class AnnagramByShort {
    public static void main(String[] args) {
        String str1 = "silent";
        String str2 = "listen";
        int len1= str1.length();
        int len2= str2.length();
        for(int i =0; i< len1; i++)
        {
            if(str1.charAt(i)>str1.charAt(i+1))
            {
                // Use the shorting method
            }
        }    
    }
}
